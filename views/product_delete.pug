extends layout

block content
    section 
        div(class='flex justify-between') 
            a(class='text-link-color go-back') Back

    section(class='border-b-0 dividerBottom')
        div
            h3 Delete
            form(method='post' action='')
                p(class='font-medium') Are you sure you want to delete the product SKU: #{product.sku}? 
                p This item will be deleted immediately and permanently. You can't undo this action.
                input(type='hidden' name='deleteId' value=product._id required=true)
                div(class='cancelDeleteRow')
                    button(type='button')
                        a(class='go-back')  Cancel
                    button(type='submit') Delete